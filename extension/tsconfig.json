{
  "extends": "../tsconfig.base.json",
  "compilerOptions": { "types": ["chrome"], "lib": ["ES2022", "DOM", "DOM.Iterable"] },
  // extension/shared is the only TypeScript source (extension/chromium and
  // extension/firefox hold just manifests); ../shared is the cross-process protocol.
  "include": ["shared", "../shared"]
}
